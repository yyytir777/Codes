#include <bits/stdc++.h>
using namespace std;


struct Trie() {
    Trie* node[10];
    bool isEnded = false;

    Trie() {
        for(int i = 0; i < 10; i++) node[i] = NULL;
    }

    void insert(char* str) {
        int num = *str - '0';
        if(node[num] == \=)
    }

    bool find(char* str) {

    }
};

int main() {
    int tc;
    cin >> tc;

    int n;
    while(tc--) {
        cin >> n;


    }
}