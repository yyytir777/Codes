#include <iostream>
#include <queue>
using namespace std;

int time[100000] = {-1,};


int main() {

    int n, k;
    cin >> n >> k;

    queue<int> q;
    q.push(n);
    p[n] = 0;

    while (!q.empty()) {

        
    }
}